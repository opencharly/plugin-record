# 2026.245.1438 — feat(record): add the run method - send a command into the recording and wait

## Summary

Adds the `record: run` method — send a command into the recording's tmux session and wait for its output to settle, verifying the session stays alive. One step replaces the cmd + explicit-settle dance for scripted flows; the command's output becomes part of the .cast.

**Changes:** schema `method` enum + `settle_ms` (default 1500ms, named const `recordRunDefaultSettle`); required modifier `text`; methods.go `recordRun`; candy description updated.

## How tested

```
$ cd candy/plugin-record && go build ./... && go vet ./... && go test ./...
ok  github.com/opencharly/plugin-record/candy/plugin-record  0.003s
```

**R10 fresh-rebuild proof (omarchy VM guest, disposable bed, calver 2026.245.1415):** the run lane executed TWICE on fresh rebuilds — [check-live] PASS 12.6s AND [check-live-rebuild] PASS 12.7s (record: start -> record: run (echo record-run-marker && uname -a, settle_ms 2000) -> record: stop); [update] PASS 5m04s (fresh-rebuild gate). The artifact was pulled host-side at the stop step: `/tmp/spike-record-run.cast` 914 B, asciinema v3, 16 events, marker echo + uname output verified INSIDE the cast (python JSON-event scan). Warnings all allowlisted; 0 errors.

The full sequence concluded: [cleanup] PASS (vm destroy) after the host stall lifted — **EXIT=0, PASS (steps=9)**; the host file `/tmp/spike-record-run.cast` (914 B) SURVIVED the VM destroy (copy-before-teardown proven: artifact pulled at the stop step during the live phases, VM destroyed in cleanup afterwards).

## Rulebook compliance

Out-of-tree plugin repo; PR-only; merged-ref pins (the bed pins v2026.242.0556; the provider built from the loader-cache source disclosed here). R3: one verb surface. R4: honest wait, no blind retries.

## Change classification

- **Change class:** feature (plugin verb method)
- **Verification gate:** unit tests green + R10 live phases green twice on fresh rebuilds (0 errors)
- **Attribution tier:** fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*