# 2026.248.1204 — record: session method (Cutover A)

## Summary

Venue-side session method (A-task-2b): the tmux path IS the transport. `session` start/status/stop reuse the existing record machinery with the venue-scoped session id as the record name; stop pulls the .cast and writes the evidence row.json (instrument/origin/verb/venue/phase/artifact). #RecordInput gains action/session_id/state_dir/venue/phase/log_dir/artifact_dir; params regenerated. The live instrument proof (same wave's plugin-check bed) exercised the session start + stop end to end; it surfaced three real fixes now in this branch.

## How tested (fresh)

```
$ cd plugin-record/candy/plugin-record && go test -count=1 ./...
ok  github.com/opencharly/plugin-record/candy/plugin-record  3.505s
$ go vet ./...  # clean
```

**LIVE (the wave's fresh-rebuild VM instrument bed — the session start/stop ran inside it):**

```
check-live PASS 1.988s | update PASS 19.887s | check-live-rebuild PASS | cleanup PASS
PASS (steps=9)
# evidence.yml row for the record session:
#   instrument: proof-instrument-vm.term, origin: session, verb: record, venue: proof-instrument-vm
#   artifact: [{kind: cast, path: .check/.../media/proof-instrument-vm.term.cast}]  (455-byte .cast pulled)
```

Proof-found fixes in this push: (1) tmux dot-name sanitize (the venue-scoped id's dots are session.window separators to tmux targets — `recordSessionName` sanitizes; the evidence row keeps the real id); (2) session stop accepts the runner's generic `artifact_dir` and derives its `.cast` target (provider-own format); (3) the pull mkdirs its host parent.

## Rulebook compliance

- **R1**: three real defects found by the live proof, fixed + unit-gated here.
- **R3**: one desugar contract; the session reuses recordStart/recordStop/recordList (no duplication).
- **R4**: sanitize is the fix, not a workaround; detach via the venue's own tmux transport.
- **R5**: no legacy surface added; N/A — no removals in this PR.
- **R8/R9**: generated `params` via the plugin gengotypes recipe; `go vet` clean; N/A — no OCI labels touched.
- **R10/ADE/SDD/RDD**: the live bed ran the changed path (session start/stop) — ADE plan is the candy's `command: true` deterministic check; N/A for SDD (no schema floor change in this repo).
- **Boundary law**: verbs stay open words; no new ops/classes.
- **R6 / git-workflow**: PR-only; no force-push.
- **Singular naming**: session/action/session_id/state_dir/artifact_dir.

## Change classification

- **Class**: code change (Go method + CUE schema + regenerated params + unit tests).
- **Verification gate**: fresh unit tests + the live session path in the wave's instrument bed (pasted).
- **Attribution**: fully tested and validated (changed runtime path — session start/stop — executed live in the instrument bed within this wave; unit-gated in this repo).

*Assisted-by: pi ollama/deepseek-v4-flash:0731 (fully tested and validated)*